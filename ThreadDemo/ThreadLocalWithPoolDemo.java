package ThreadDemo;

import java.util.concurrent.Executor;

public class ThreadLocalWithPoolDemo {
    private static final ThreadLocal<String> threadLocal = new ThreadLocal<>();

    public static void main(String[] args) {
      //  Executor
        for (int i = 0; i < 4; i++) {

        }
    }
}
